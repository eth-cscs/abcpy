language: python

python:
  - "3.5"

addons:
  apt:
    packages:
      - gfortran
      - libboost-random-dev
      - libpython3-dev
      - swig
  
install:
  - pip install -r requirements.txt

script:
  - make test

